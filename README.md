# acppred

By Diana

a tool to predict anticancer peptides

## Setup

```
$ Este programa utiliza miniforge, com versões para todas as arquiteturas Linux, bem como para MacOS.
Cookiecutter requer Python 3.3 ou posterior (32 bits ou 64 bits) ou Anaconda 3 4.2 ou posterior (32 bits ou 64 bits). Foi configurado Conda no Git Bash. Na base e o ambiente onde todos os pacotes são contidos (test,...). Se você estiver interessado em ver quais pacotes estão instalados basta digitar o comando conda list. 

$ This program uses miniforge, with versions for all Linux architectures, as well as MacOS.
Cookiecutter requires Python 3.3 or later (32-bit or 64-bit) or Anaconda 3 4.2 or later (32-bit or 64-bit).
At the base and the environment where all the packages are contained (test,...). If you are interested in seeing what packages are installed just type the conda list command
```
